# Vue 元件重構為 Tailwind CSS

**時間**: 2026-05-04_144147  
**模型**: claude-sonnet-4.6

---

## 問題摘要

將 `/mnt/d/github/jenkins-sample/frontend/src/` 下所有 Vue 元件從自定義 CSS class 重構為 Tailwind CSS utility classes，並移除所有 `<style>` 區塊。

---

## 回答重點

### 技術背景
- Vue 3 Composition API
- Tailwind CSS v4（via @tailwindcss/vite）
- PrimeVue v4
- Vue Router

### 設計規範
- 主色：blue (#3b82f6)，中性色：gray
- Header：深色背景 (bg-gray-900) + 白色文字導覽
- 卡片：white bg、rounded-lg、shadow-md、hover effects
- 按鈕：bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded
- 表單輸入：border border-gray-300 rounded px-3 py-2 focus:ring-2 focus:ring-blue-500
- 表格：w-full border-collapse，thead bg-gray-50
- Admin sidebar：w-64 bg-gray-800 text-white

---

## 執行操作

重構以下 18 個檔案的 `<template>` 區塊，移除所有 `<style>` 區塊：

### Components (7 個)
1. `src/components/CartItem.vue` — flex 佈局、remove 按鈕改為文字連結樣式
2. `src/components/ProductCard.vue` — 卡片樣式、藍色價格、Add to Cart 按鈕
3. `src/components/admin/OrderDetail.vue` — Modal 覆蓋層、items 列表
4. `src/components/admin/ProductForm.vue` — Modal 表單、labeled inputs、Save/Cancel 按鈕
5. `src/components/layout/AdminLayout.vue` — 側邊欄 (w-64 bg-gray-800) + 主內容區
6. `src/components/layout/Footer.vue` — 簡潔 footer 樣式
7. `src/components/layout/Header.vue` — 深色 header、nav 連結、Login/Logout 按鈕

### Pages (11 個)
8. `src/pages/CartPage.vue` — 購物車列表 + 結帳摘要
9. `src/pages/CheckoutPage.vue` — 結帳表單、訂單摘要卡片
10. `src/pages/HomePage.vue` — 置中歡迎頁、CTA 按鈕
11. `src/pages/LoginPage.vue` — 居中登入卡片
12. `src/pages/ProductDetailPage.vue` — flex 商品詳情 + 圖片
13. `src/pages/ProductsPage.vue` — grid 商品列表 (1-4 欄響應式)
14. `src/pages/RegisterPage.vue` — 居中註冊卡片
15. `src/pages/UserProfilePage.vue` — 個人資料 + 訂單列表
16. `src/pages/admin/AdminOrdersPage.vue` — 訂單管理表格 + status select
17. `src/pages/admin/AdminProductsPage.vue` — 商品管理表格 + Add/Edit/Delete
18. `src/pages/admin/AdminUsersPage.vue` — 用戶管理表格 + role badge

---

## 驗證結果

- 所有 `<style>` 區塊已移除（grep 確認無殘留）
- 所有非 Tailwind 自定義 class 已移除
- 所有 `<template>` 結構完整（Python 驗證）
- `<script setup>` 邏輯完全未變動

---

## 產出檔案

重構了 18 個 Vue SFC 檔案，無新增檔案。
